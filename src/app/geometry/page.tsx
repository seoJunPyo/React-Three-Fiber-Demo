import { Box } from '@/components/Geometry';
import React from 'react';

const page = () => {
  return (
    <div>
      <h2 className="text-4xl font-bold">Box Geometry</h2>
      <Box />
    </div>
  );
};

export default page;
